import 'package:flutter/material.dart';

defaultTextFormFieldTextStyle(ThemeData theme) {
  return TextStyle(
    color: theme.colorScheme.secondary,
    fontSize: 16,
    fontWeight: FontWeight.w500,
  );
}

defaultElevatedButtonTextStyle(ThemeData theme) {
  return TextStyle(
    color: theme.colorScheme.secondary,
    fontSize: 16,
    fontWeight: FontWeight.w500,
  );
}
